# Create a data directory outside the deployed app so SQLite DB survives redeploys.
# After deploy, set: eb setenv AUTH_DB_PATH=/var/app/data/auth.db
commands:
  01_create_data_dir:
    command: "mkdir -p /var/app/data && chown webapp:webapp /var/app/data"
    ignoreErrors: false
