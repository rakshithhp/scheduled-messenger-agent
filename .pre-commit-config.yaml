# Run tests before every commit. Install with: pre-commit install
# Then tests run automatically on git commit; use --no-verify to skip.
repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: python3 -m pytest tests/ -q
        language: system
        pass_filenames: false
